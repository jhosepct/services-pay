server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://gituhub.com/jhosepct/config-repo
          search-paths: config-data
  application:
    name: config-service
